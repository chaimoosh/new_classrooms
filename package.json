{
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^2.0.1",
    "mocha": "^2.4.5",
    "supertest": "^3.3.0"
  },
  "scripts": {
    "start": "SET NODE_ENV=dev && node server.js",
    "test": "mocha --timeout 10000"
  },
  "dependencies": {
    "chai": "^3.2.0",
    "chai-http": "^1.0.0",
    "es6-promise": "^4.2.5",
    "isomorphic-fetch": "^2.2.1"
  }
}
